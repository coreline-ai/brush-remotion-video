# implement_20260718_212631.md

작성 일시: `2026-07-18 21:26:31 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

KIMI-K3 프로모 영상 분석([KakaoTalk_Video_2026-07-18-19-03-47_분석.md](./KakaoTalk_Video_2026-07-18-19-03-47_분석.md))에서 도출한 **다크 프로모 위젯 36종(W1–W36)** 을 이 레포의 재사용 자산으로 편입한다. 위젯을 파라메트릭 Remotion 컴포넌트로 만들고, 카탈로그·스키마·갤러리 회귀·스킬 패키징의 4층 자산화 구조로 고정한다.

## 개발 범위

- **신규 분리 라인** `src/promo/` — 다크 프로모 위젯 전용. 기존 붓/펜 위젯(`src/widgets/`, 화이트 카드 15종)과 코드·스키마·테스트 모두 분리한다. (`FullColorMotionSequence`가 분리 라인으로 들어간 선례를 따른다)
- 설계 패턴은 기존 관례와 **동형**: zod discriminated union 스키마 + type→body registry + placeholder 안전 렌더 + vitest.
- 디자인 토큰: 분석 문서 §2.2의 컬러/타입/모션 토큰(`#4a7fff` 데이터 블루, `#f5b73c` CTA 골드, `#e2483d` 라이벌 레드, `#0a0e1a` 다크 배경).
- 갤러리 컴포지션 `PromoWidgetGallery` — 등록 위젯 전량을 한 화면에 렌더하는 시각 회귀 기준.

## 제외 범위

- 기존 `src/widgets/` 15종·`WidgetSchema`·`WidgetLayer` 수정 (기존 계약 TC-5.1은 15종 고정 — 건드리지 않는다)
- 3D 에셋(구체·게임기·파티클, R3F 도입), 게임 푸티지 대체 제작, 오디오(TTS/BGM) — 제안서 F2/F3 영역은 후속 워크스트림
- KIMI-K3 프로모 풀 영상(30씬) 조립 — 위젯 자산화가 이 문서의 범위
- `bin/build.py` 파이프라인 연동 및 project.yaml 계약 확장

## 참조 문서

- [KakaoTalk_Video_2026-07-18-19-03-47_분석.md](./KakaoTalk_Video_2026-07-18-19-03-47_분석.md) — 위젯 36종 census·디자인 토큰·모션 문법의 근거
- [KIMI-K3_에셋_재현_제안서.md](./KIMI-K3_에셋_재현_제안서.md) — 자산화 4층 구조·컴포넌트 매핑
- [src/widgets/registry.tsx](../src/widgets/registry.tsx) · [src/widgets/shared.tsx](../src/widgets/shared.tsx) — 따라야 할 registry/토큰 패턴
- [tests/widgets.test.ts](../tests/widgets.test.ts) — 따라야 할 테스트 패턴

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다. (P1–P3은 신규 의존성 0)

## Phase 상태 요약

- [x] Phase 1 완료 — 코어 위젯 4종 + 토큰 + 갤러리
- [x] Phase 2 완료 — 카탈로그/스키마 자산화 + 정합 테스트
- [x] Phase 3 완료 — 잔여 위젯 패밀리 단위 편입 (27종 → 총 31종)
- [x] Phase 4 완료 — 스킬 패키징 (promo-widget-video, 13번째 스킬)
- [x] Phase 5 완료 — 연출 계층 A(씬 시퀀서·전환 3종)·B(무대 5종·idle 드리프트)·C(키네틱 타이포·플래시 재보정) — D(TTS 내레이션)는 미착수 유보

## QA 관점

- [x] 값 경계: gauge value가 min/max 밖, leaderboard rows 0개/1개, countUp from==to, bar 눈금 미지정 등 경계값에서 크래시 없이 클램프/기본값 처리되는지. → vitest 경계 테스트로 검증 (normalizeRatio·gaugeState·litCount·flapChar 등)
- [x] 결정성: 동일 frame 입력 → 동일 출력 (Math.random/Date 사용 금지, Remotion 결정성 준수). → `hash01` sin 해시로 대체, flapChar 동일 프레임 동일 문자 테스트
- [x] 미등록 타입: placeholder 렌더 + console.warn (기존 registry 계약과 동일 동작). → vitest 검증
- [x] 회귀 리스크: 기존 `src/widgets/`·`schema.ts`를 import하거나 수정하지 않았는지 (분리 라인 보장). 기존 테스트 전량 통과 유지. → vitest 78/78 (기존 포함)
- [x] 갤러리 렌더가 studio·render CLI 모두에서 프레임 결정적으로 동작하는지. → still·render 산출물로 확인

## 검증(실체 증명) 기록 — 2026-07-18

커밋 전 실체 증명을 별도 수행. 전문: [VERIFICATION_20260718.md](./VERIFICATION_20260718.md)

- [x] 증명 1 갤러리 25초 풀 렌더 (mp4 실체)
- [x] 증명 2 video-auditor 독립 검수 — 최초 FAIL(무음) → 라이선스 BGM −23 LUFS 믹스(레포 관례, 코드 무변경) 후 **양쪽 PASS**
- [x] 증명 3 신규 props E2E — demo에 없는 새 씬이 스킬 계약만으로 성립 (자산 재사용성 증명)
- [x] 증명 4 원본 대조 시트 / 증명 5 게이트 일괄 로그
- [ ] 증명 6 **사용자 육안 판정** (mp4 재생) — 이것이 채워지기 전 커밋 금지

## Phase 1. 코어 위젯 4종 + 디자인 토큰 + PromoWidgetGallery

### 목표

분석 문서에서 사용 빈도·ROI가 가장 높은 코어 4종(W1 게이지, W2/W3 바, W5 리더보드, W8/W9 카운트업)을 파라메트릭 컴포넌트로 구현하고, 갤러리 컴포지션으로 시각 확인 가능하게 한다.

### 책임 단위

| 파일 | 책임 |
|---|---|
| `src/promo/tokens.ts` | 디자인 토큰(컬러·radius·stroke·타입·모션 이징) 단일 정의 |
| `src/promo/schema.ts` | `PromoWidgetSchema` zod discriminated union (gauge·statBar·leaderboard·countUp) + 갤러리 props |
| `src/promo/bodies/Gauge.tsx` | W1a 니들 / W1b 필-아크 게이지 (kind로 통합) |
| `src/promo/bodies/StatBar.tsx` | W2 수평 진행 바(눈금·골드 라벨) / W3 VS 모드 |
| `src/promo/bodies/Leaderboard.tsx` | W5 리더보드(행 populate·하이라이트 행·리오더) |
| `src/promo/bodies/CountUp.tsx` | W8 카운트업 / W9 배수(prefix) — 오도미터 |
| `src/promo/registry.tsx` | type→body 매핑 + placeholder (기존 패턴 동형) |
| `src/promo/PromoWidgetGallery.tsx` | 등록 위젯 전량 그리드 렌더 + kicker/subtitle 고정 슬롯 시연 |
| `src/Root.tsx` | `PromoWidgetGallery` 컴포지션 등록 (등록부 외 로직 금지 준수) |

### 구현 태스크

- [x] `src/promo/tokens.ts` — 분석 §2.2 토큰 정의
- [x] `src/promo/schema.ts` — 4종 discriminated union + strict + 기본값
- [x] `Gauge` 구현 (needle: 바늘 스윕+카운트업 / fill-arc: 아크 채움+틱 스포크 점등+골드 테일)
- [x] `StatBar` 구현 (progress: 좌→우 필+눈금+우단 라벨 / vs: 양측 바+중앙 VS 배지)
- [x] `Leaderboard` 구현 (행 stagger 진입, 하이라이트 행 풀폭 블루, 점수 우측정렬 tabular, 리오더 이동)
- [x] `CountUp` 구현 (ease-out 카운트업, prefix/suffix, 언더라인 룰, 캡션)
- [x] `registry.tsx` + `PromoWidgetGallery.tsx` + Root.tsx 등록 (+ 공용 `shared.tsx`: PromoShell·promoProgress·normalizeRatio)
- [x] `tests/promo-widgets.test.tsx` 작성 (14 테스트)

### 자체 테스트

- [x] `npm run typecheck` 통과
- [x] `npm test` 전량 통과 — 11 파일 73 테스트 (기존 회귀 0)
- [x] 신규 테스트: registry 4종 등록 / 미등록 타입 placeholder / 스키마 strict 거부 / 경계값(클램프) / 프레임 진행에 따른 값 단조 증가
- [x] `npx remotion compositions`에 `PromoWidgetGallery` 노출 확인 (1920×1080 · 30fps · 150f)
- [x] 갤러리 스틸 렌더 f120(정착)·f30(진행 중) 육안 대조 — 니들 스윕·아크 채움+골드 테일·리더보드 리오더 상승·바 필·stagger 진입 모두 원본 문법과 정합

### 이슈 및 수정

- [x] 발견 이슈 없음

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 카탈로그/스키마 자산화 + 정합 테스트

### 목표

위젯을 에이전트가 발견·호출할 수 있는 색인으로 고정한다 (bgm/instruments 카탈로그 패턴 동형).

### 구현 태스크

- [x] `assets/promo-widgets/catalog.json` — id·family·component·props·demos·originWidgets(W-번호) 4종 등록
- [x] `assets/promo-widgets/catalog.schema.json` (draft-07, additionalProperties 금지)
- [x] `pipeline/tests/test_promo_widget_catalog.py` — 6 테스트: 스키마 유효성 / 참조 파일 실재 / id 유일성 / registry·PromoWidgetSchema와 id 집합 일치 / demo type 정합 / 갤러리 단일 근원(하드코딩 회귀 방지)
- [x] 갤러리 데모 props를 catalog의 demos에서 파생 (tsconfig `resolveJsonModule` 추가, `GALLERY_DEMO_WIDGETS` = catalog flatMap → zod parse)

### 자체 테스트

- [x] pytest 신규 6개 통과 + 전량 실행: 446 passed / 1 failed — 실패는 `test_melo_adapter...`(mecab 패키지 미설치, 환경 의존)로 **이 워크스트림과 무관한 사전 존재 실패** (해당 파일 working tree clean)
- [x] catalog.json이 catalog.schema.json에 대해 VALID (jsonschema)
- [x] typecheck + vitest 73개 전량 통과 (JSON import 전환 후에도 기존 "데모 전량 parse" 테스트가 카탈로그를 검증)
- [x] 갤러리 스틸 f120 재렌더 — P1과 픽셀 동일 (단일 근원 전환의 시각 회귀 0)

### 이슈 및 수정

- [x] 발견 이슈 없음 (melo TTS pytest 실패는 사전 존재·환경 의존으로 기록만 남김)

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 3. 잔여 위젯 패밀리 단위 편입

### 목표

분석 문서 §4의 잔여 위젯을 패밀리 단위로 순차 편입한다. 각 패밀리 완료 시 catalog·갤러리·테스트 동시 갱신.

### 구현 태스크

- [x] 패밀리 3+4 잔여: RankLadder(W6) · NumberLinePlot(W7) · PriceTag(W10)
- [x] 패밀리 5: NodeGraph(W11) · CurvePlot(W12) · HeatmapGrid(W13) · ParticleField(W14) · Oscilloscope(W15) · FlowDiagram(W16) · StrataDiagram(W17)
- [x] 패밀리 6: LogCard(W18) · Terminal(W19) · ChecklistPanel(W20) · CalloutPanel(W21) · PlatformSelector(W22)
- [x] 패밀리 7: Kicker(W23) · PillBadge(W24) · SportsCallout(W25) · Marquee(W26) · SplitFlap(W27) · TimelineScrubber(W28) · FrameBrackets(W29) · Subtitle(W31) · ChevronBadges(W32) · DateFlip(W33) · TicketProp(W34) · LogoLockup(W36)
- [x] 스키마 31종 union + registry 31종 + catalog 31종(demos에 갤러리 `page` 메타) + 갤러리 5페이지화(`PAGE_FRAMES`=150, duration=pages×150) + 테스트
- [x] 결정성 준수: 파티클/스플릿플랩은 `hash01`(sin 해시)로 의사난수 — Math.random/Date 미사용

(W30 게임 HUD·W35 3D 구체는 F2/F3 의존 — 제외 범위, 후속 워크스트림)

### 자체 테스트

- [x] typecheck 통과 + vitest 78개 전량 통과 (registry↔catalog 31종 대조, 전 타입 demo 시연 보장, litCount/flapChar/marqueeOffset 경계값)
- [x] pytest: catalog 정합 6개 통과 + 전량 446 passed (melo TTS 1건은 P2에서 기록한 사전 존재·환경 의존 실패)
- [x] 갤러리 5페이지 스틸(f120/270/420/570/720) 육안 확인 — 31종 전량 시연

### 이슈 및 수정

- [x] 히트맵 `89.3` 우측 잘림 → demo 배치 축소(y170·h290)로 해소
- [x] 래더 토큰이 상단 `RANK` 라벨과 겹침 → 라벨 제거 + 도착 위치 4% 마진
- [x] 갤러리 렌더 테스트가 구 props(`widgets`) 참조 → `pages` 기반으로 수정

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 4. 스킬 패키징

### 목표

`skill/promo-widget-video/` 프로파일 스킬로 소비 인터페이스를 만든다 (코드 사본 0, SKILL.md + agents/openai.yaml + skill/catalog.json 등록).

### 구현 태스크

- [x] `skill/promo-widget-video/SKILL.md` — 위젯 카탈로그 참조·props 계약·실행 순서·에이전트 행동 계약 5조
- [x] `skill/promo-widget-video/agents/openai.yaml` (catalog interface와 정확 일치)
- [x] `skill/catalog.json` 등록 (13번째, role=production·status=specialized·entrypoint=`src/promo/PromoWidgetGallery.tsx`) + `skill/catalog.schema.json` skillId enum·skills min/maxItems 12→13
- [x] `bin/skill-catalog.py generate-readme` — README 스킬 테이블 갱신
- [x] `bin/install-skills.sh --target claude` — `~/.claude/skills/promo-widget-video` 심링크 설치

### 자체 테스트

- [x] `bin/skill-catalog.py check` **PASS: skills=13, agents=13, README=sync**
- [x] test_skill_catalog + test_install_skills + test_promo_widget_catalog **25 passed** (스킬 수 고정 회귀 방어 12→13 갱신 포함)

### 이슈 및 수정

- [x] catalog.schema.json의 `skills` min/maxItems=12 고정 → 신규 등록이 schema 위반으로 FAIL → 13으로 상향 (스킬 추가 시 함께 올리는 의도된 회귀 방어)
- [x] test_skill_catalog의 스킬 수 하드코딩 2곳(12) → 13으로 갱신

### 완료 조건

- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 워크스트림 종료

## 최종 결과 요약

KIMI-K3 프로모 영상 분석에서 도출한 다크 프로모 위젯을 4층 자산화 구조로 리포에 편입 완료.

| 층 | 산출물 |
|---|---|
| ① 컴포넌트 | `src/promo/` — 토큰·스키마·shared 아톰 + bodies 29파일 (**위젯 32종**, zod strict) |
| ② 계약 | `assets/promo-widgets/catalog.json` + `catalog.schema.json` — id·family·originWidgets(W-번호)·props·demos(page 메타) |
| ③ 증거 | `PromoWidgetGallery` 6페이지(900f) 시각 회귀 컴포지션 + vitest 88 + pytest(catalog 정합 6) |
| ④ 스킬 | `skill/promo-widget-video/` — 13번째 정식 스킬, catalog check PASS, 심링크 설치 |
| ⑤ 연출 계층 (P5) | `src/promo/scene/` — `PromoScene` 프로덕션 컴포지션: 가변 씬 길이·전환 3종(light-sweep/white-flash/push-in)·무대 5종(orb/grid/particle-dust/spotlight/glow)+tint·flashAt 비트(원본 계측 peak 0.5)·씬별 kicker/subtitle·idle 드리프트·heroTitle 키네틱 타이포 |

- 정합 3중 게이트: catalog ↔ registry ↔ zod schema id 집합 일치를 vitest·pytest 양쪽에서 강제
- 결정성: Math.random/Date 미사용 (`hash01` sin 해시)
- 제외(후속 과제): W30 게임 HUD·W35 3D 구체(F2/F3), 가변 씬 길이·오디오 동기, bin/build.py 연동

## Phase 5. 연출 계층 (Direction Layer) — 2026-07-19 사용자 승인으로 범위 추가

### 배경

Cowork 실전 렌더에서 원본 대비 품질 갭 확인 (전환 하드컷·평면 배경·5초 고정·정지 홀드).
원본 전환 4구간을 프레임 단위 재분석 → 화이트 플래시는 "전프레임 노출 리프트 베일(peak ~0.4, 1–3f)"이며
씬 중간 비트에도 반복 사용됨을 확인. 이 증거 기반으로 구현한다.

### 스코프 (사용자 선택: A+B 먼저, 재렌더 중간 판정)

- [x] **P5-A** `PromoSceneSequence` 프로덕션 컴포지션 — 씬별 `durationInFrames` 가변 + 진입 전환 3종(light-sweep·white-flash·push-in) + 씬 내부 `flashAt[]` 비트 + 씬별 kicker/subtitle 1급 지원 (`src/promo/scene/` — transitions.ts 순수 함수 + PromoSceneSequence.tsx, Root에 `PromoScene` 등록)
- [x] **P5-B** Stage 무대 프리셋 — glow·particle-dust·grid·orb·spotlight + tint 컬러 워시(blue/warm) + 워터마크 글리프 + PromoShell idle 드리프트(sin 위상 x-분산, 진폭 2.2px) + 등장 스케일 0.985→1
- [x] 테스트: `tests/promo-scene.test.tsx` 9개 — 씬 오프셋/activeScene 클램프/전환 창/플래시 커브(peak 0.4·상승 2f·감쇠 4f 계측 스펙)/sweepX/스키마 strict. typecheck + vitest 87 전량 통과
- [x] **중간 판정 자료**: Cowork 재렌더 `output/promo-verify/cowork-v2-bgm.mp4` (32.5초·가변 씬 길이 4~5.5초·전환 3종·무대 5종·플래시 비트 7개)

- [x] **P5-C** (v2 중간 판정 "더 필요"로 사용자 승인) — ① `heroTitle` 키네틱 타이포 위젯 32번째 편입(4종 세트: 스키마+body+registry+catalog page6 데모, 거대 scaleFrom 1.5 → settleFrames 수축 + 글로우, 원본 KIMI K3 타이틀 문법) ② 플래시 재보정: peak 0.4→0.5·상승 2f→1f (원본 스파이크 diff ~37% 역산 — v2는 ~16%로 절반 수준이었음). vitest 88 전량 통과
- [x] Cowork v3: 타이틀 씬을 heroTitle 키네틱(CLAUDE/COWORK + orb 무대 + 정착 직후 플래시 비트)으로 교체 — `output/promo-verify/cowork-v3-bgm.mp4`. 프레임 검증: 거대 등장(f11)→수축(f13)→플래시 피크(f16)→정착(f40) 원본 시퀀스 재현. orb-타이틀 겹침 발견 → y 조정으로 해소

(제외 유지: P5-D 내레이션 동기 — 판정 후 결정)

### 이슈 및 수정

- [x] **auditor 장르 충돌 발견·반증**: v2 검수에서 FAIL 5(spike=flashAt 비트, hardcut=white-flash 컷). **원본 KIMI 영상을 같은 auditor로 검수 → FAIL 47(spike diff ~37%·hardcut 86%)** — 즉 이 FAIL들은 결함이 아니라 컷 기반 모션그래픽 장르 특성이며, auditor 기준은 브러시 리빌 장르용. v2의 diff(~16%)는 원본(~37%)보다 절제된 수준. 증거: `output/promo-verify/audit-original-kimi/`

## 잔여 리스크 / 후속 과제

- ~~페이지당 150프레임(5초) 고정~~ → **P5-A로 해소** (PromoScene 가변 씬 길이)
- 폰트는 시스템 폰트 스택 근사 — 원본 폰트 특정 불가 (제안서 §11)
- **P5-D 미착수**: TTS 내레이션 씬 동기 (Supertonic) — 사용자 판정 후 결정
- **BGM 정책 주의**: 검증 mp4들의 BGM(pixabay-digital-ambient-meditation)은 `youtubeAllowed: false` — 내부 검증용으로만 유효. **배포 시 YT-OK 곡(9종) 교체 또는 piano-bgm 자작 필수** (2026-07-19 사용자 질의로 발견, mux 시 플래그 미확인이 원인)
- **사용자 육안 판정 미완**: cowork-v3-bgm.mp4 최종 판정 대기 (VERIFICATION 확인란)
- 이 워크스트림 산출물 전체가 미커밋 상태 — P1~P5 코드 + 계획서/분석/제안서/검증(external_samples/)을 함께 커밋해야 catalog entrypoint가 배포본에서 유효

## 확인 필요 사항 / 결정 기록

- **결정**: 기존 `src/widgets/`에 편입하지 않고 `src/promo/` 분리 라인으로 간다. 근거 — ① 기존 15종 테스트가 개수를 고정(TC-5.1), ② 화이트 카드 CardShell 시각 언어와 다크 프로모 언어가 양립 불가, ③ FullColorMotion 분리 선례.
- **결정**: 갤러리 규격은 1920×1080·30fps(레포 표준). 원본(720p/24)이 아닌 레포 표준을 따른다 — 자산은 벡터라 해상도 독립.
