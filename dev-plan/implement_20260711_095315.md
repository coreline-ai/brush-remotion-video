# implement_20260711_095315.md

작성 일시: `2026-07-11 09:53:15 KST`

이 문서는 이번 개발의 범위를 고정하고, 구현이 목적 밖으로 확장되지 않도록 하기 위한 작업 문서다.

## 개발 목적

brush_remotion_video 신규 프로젝트(완전 새 제작, 이전 아님)의 첫 구현 구간으로,
**Phase 0(스캐폴드 + Zod 스키마 v1)과 Phase 1(브러시 리빌 코어)** 를 완료한다.
완료 시점에 "routes JSON을 입력하면 기존 new-video-gen과 동등 화질의 수묵화 리빌 영상이
새 리포에서 렌더된다"가 증명되어야 한다 (골든 스틸 픽셀 diff ≤ 2%).

## 개발 범위

- Remotion 4.x + React 19 단독 스캐폴드 (`package.json`, `tsconfig.json`, `remotion.config.ts`, `.gitignore`, `git init`)
- `src/schema.ts` — Zod 스키마 v1 (`schemaVersion: 1`, 유일한 진실) + JSON Schema 내보내기 스크립트
- `src/index.ts`, `src/Root.tsx` — 컴포지션 2종(`BrushLandscape` 1920×1080, `BrushPortrait` 1080×1920) + `calculateMetadata`
- 리빌 코어 구현 (책임 단위별 분리):
  - `src/lib/geometry.ts` — `toPath()`, `pointOnPolyline()` (경로 좌표 계산만)
  - `src/lib/dynamics.ts` — `hash01()`, `jitterPoints()`, `normalizeBrushDynamics()`, `buildDynamicStrokes()` (deterministic 스트로크 변형만)
  - `src/lib/easing.ts` — `sharedProgress()` + easeDraw/easeTravel/easeDevelop 상수 (진행률 계산만)
  - `src/scene/RevealLayer.tsx` — 2단계 리빌(faint 스트로크 → develop) + edgeFeather/linearDraw/prewash/outro
  - `src/scene/CursorLayer.tsx` — `getPenPose()` + 붓 커서 렌더
  - `src/scene/BrushScene.tsx` — 레이어 조립 전용 (로직 없음)
- 골든 테스트 기반: `data/golden-single/`(winter-snow-pine-demo 에셋 차용) + `tests/golden/diff.py`(픽셀 diff, 임계 2%)
- 단위 테스트: vitest (`tests/schema.test.ts`, `tests/lib.test.ts`)

## 제외 범위

- Phase 2~6 전체 (자막/타이틀/이펙트/시퀀서, Python 파이프라인, build.py, 위젯 registry, 스킬 재패키징)
- new-video-gen 리포의 어떤 파일도 수정하지 않는다 (읽기 전용 스펙 참조)
- 구세대 스키마 하위호환 필드 (`meta{}`, 레거시 `widgets[]` 등)
- 위젯/컨테이너 렌더 (스키마에 자리만 예약하지 않는다 — Phase 5에서 union으로 추가)
- routes JSON 생성 알고리즘 (Phase 3) — 이번 구간은 기존 routes JSON을 fixture로 소비만 한다
- 스킬 내장 구버전 코드 사본(390줄) 참조 — 스펙은 항상 라이브 코드(`new-video-gen/src/remotion/BrushDrawScene.tsx` 761줄)에서

## 참조 문서

- [상세 구현계획서](../docs/impl-plan-brush-remotion-video.md) — 전체 Phase 0~6 설계·테스트 케이스의 원본. 본 문서의 Phase 1·2는 계획서의 Phase 0·1에 대응
- [MIGRATION-PLAN.md](../MIGRATION-PLAN.md) — 분석 자료 (스키마 계보, 파일 매핑). 이전 전략 자체는 폐기됨
- 스펙 소스(읽기 전용): `new-video-gen/src/remotion/BrushDrawScene.tsx` — 타입 L36–107, sharedProgress L200, pointOnPolyline L210, hash01/jitterPoints L117/L144, buildDynamicStrokes L164, normalizeBrushDynamics L128, 이징 상수 L109–112, PenCursor L256
- 골든 입력: `new-video-gen/data/winter-snow-pine-demo/render-props.json` + `new-video-gen/public/winter-snow-pine-demo/`

## 공통 진행 규칙

- 각 Phase는 앞선 Phase의 자체 테스트 완료 후에만 시작한다.
- 구현 중 발생한 이슈는 해당 Phase에서 수정하고 기록한다.
- 체크박스 상태를 실제 진행 상태와 맞게 업데이트한다.
- 문서에 없는 범위 확장은 하지 않는다.
- 요구사항이 불명확하면 가능한 케이스를 나누고 확인 후 진행한다.
- 한 기능은 유지보수 가능한 최소 책임 단위로 나누고, 각 단위의 역할과 경계를 계획에 적는다.
- 계획 밖 옵션/확장/부가 기능을 만들기 위한 분리는 하지 않는다.
- 공통화는 계획 범위 안의 중복을 줄일 때만 수행하고 과도한 추상화는 피한다.
- 기존 프로젝트 API, 공식 SDK, 표준 라이브러리를 우선 검토하고 새 의존성은 범위상 필요할 때만 포함한다.
- (본 프로젝트 특칙) 코드 복사 금지 — 로직은 새로 작성하되 이징 곡선·기본값·수식은 라이브 코드에서 채택한다.

## Phase 상태 요약

- [x] Phase 1 완료 (스캐폴드 + Zod 스키마 v1) — 2026-07-11
- [x] Phase 2 완료 (리빌 코어 + 골든 파리티 0.06~0.17%) — 2026-07-11

## QA 관점

- [x] 실패 케이스와 경계값 검토 완료: strokes 빈 배열(크래시 없음), routes 부재(명시 에러), cue from 타입 오류(경로 포함 ZodError), schemaVersion 누락(거부)
- [x] 회귀 게이트 확립: 골든 스틸 5프레임 diff ≤ 2% (실측 0.057~0.174%) — 기준 갱신은 `diff.py --update`로만
- [x] deterministic 검증: buildDynamicStrokes seed 재현성(vitest) + 동일 프레임 2회 렌더 diff 0.0000%
- [x] 렌더 편차 확인: 2회 재렌더 diff 0% — 폰트/Chromium 흔들림 없음 (텍스트 없는 씬 기준)
- [x] Zod 에러 메시지 필드 경로 포함 확인 (`scenes.0.cues.0.from`)

## Phase 1. 스캐폴드 + Zod 스키마 v1

### 목표
- 빈 컴포지션이 렌더되는 Remotion 단독 프로젝트를 만들고, Zod 스키마 v1을 유일한 진실로 확정한다.

### 구현 태스크
- [x] `package.json` 작성 — remotion/@remotion/cli 4.0.435 동일 버전 고정(참조 시스템과 일치), react 19, zod, vitest, zod-to-json-schema
- [x] `tsconfig.json`(strict, bundler resolution, alias 없음) + `remotion.config.ts`(jpeg, h264/yuv420p, CRF 18 — new-video-gen 설정값 채택) + `.gitignore` + `git init`
- [x] `src/schema.ts` — CueSchema, RoutesDataSchema(기존 routes 포맷 호환, meta passthrough), BrushDynamicsSchema, TopTitleSchema, SubtitleStyleSchema, NaturalEffectsSchema, SceneSchema, RenderPropsSchema(schemaVersion: 1). 튜닝 기본값(faint 0.6 등)을 스키마에 단일 정의
- [x] `scripts/export-schema.ts` — `schema/render-props.schema.json` 내보내기 (`npm run export-schema`)
- [x] `src/index.ts`(registerRoot) + `src/Root.tsx`(BrushLandscape/BrushPortrait + calculateMetadata에서 parse → 기본값 채워 전달)
- [x] `src/scene/BrushScene.tsx` placeholder(paper 배경색만) + 1초 렌더 스모크

### 자체 테스트
- [x] `npx tsc --noEmit` 에러 0건
- [x] `npx remotion compositions src/index.ts`에 컴포지션 2종 노출 (BrushLandscape 1920×1080 / BrushPortrait 1080×1920)
- [x] `npx remotion render src/index.ts BrushLandscape output/smoke.mp4 --frames=0-29` 성공, ffprobe 1920×1080 · 30fps · 30프레임 확인
- [x] vitest 6건 통과: 유효 props parse+기본값 / schemaVersion 누락 거부 / `cues[0].from` 문자열 → 경로 포함 ZodError / 빈 scenes 거부 / routes meta passthrough / 빈 strokes 허용

### 이슈 및 수정
- [x] 발견 이슈 없음

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 다음 Phase 진행 가능

## Phase 2. 리빌 코어 + 골든 파리티

### 목표
- routes JSON → 스트로크 리빌(faint) → develop(선명화) 2단계가 기존 렌더와 동등 화질(diff ≤ 2%)로 재현된다.

### 구현 태스크
- [x] `src/lib/geometry.ts` — toPath, pointOnPolyline (라이브 수식 채택, 순수 함수)
- [x] `src/lib/dynamics.ts` — hash01, jitterPoints, normalizeBrushDynamics, buildDynamicStrokes (seed 기반 deterministic)
- [x] `src/lib/easing.ts` — sharedProgress(72% 조기완성 스냅 포함) + easeDraw/easeTravel/easeDevelop
- [x] `src/scene/RevealLayer.tsx` — faint 스트로크 리빌 + develop + edgeFeather 마스크/linearDraw/prewash 4종/outro 3종
- [x] `src/scene/CursorLayer.tsx` — getPenPose + 붓 이미지 커서(tipx/tipy 앵커, +180° 보정, 미세 흔들림)
- [x] `data/golden-single/` 구성 — routes JSON·composed.png·brush.png 복사 + v1 props, `BrushScene.tsx` 조립(routes 로드 시 Zod 검증)
- [x] `tests/golden/` — baseline 스틸 5프레임(f20 프리워시/f100/f200/f270 develop/f295 완료) + `diff.py`(임계 2%, `--update` 지원)

### 자체 테스트
- [x] vitest 21건 통과: pointOnPolyline 경계값/경로길이 보간, buildDynamicStrokes seed 재현성·타임라인 스케일, sharedProgress 단조성/클램프/스냅, normalize 클램프
- [x] 골든 300프레임 렌더 성공 + **baseline 스틸 5프레임 diff 0.057~0.174% (게이트 2%의 1/10 수준)**
- [x] 변형 구분: linearDraw off 0.68% / edgeFeather 0 0.43% / prewash 0 3.03% — 전부 diff > 0
- [x] 에러 경로: strokes 빈 배열 → 크래시 없음(base 대비 3.95% = 배경만) / routes 누락 → "routes 누락: scene" 명시 에러 렌더 (스틸 육안 확인)
- [x] 재현성: 동일 프레임 2회 렌더 diff 0.0000% (deterministic 확인)

### 이슈 및 수정
- [x] **routes 스트로크 타이밍이 소수 프레임(8.07 등)** — 스키마의 int 제약이 실측과 불일치 → `fractionalFrame`(min 0)으로 완화, 테스트에 소수 케이스 반영
- [x] diff.py의 PIL/numpy가 시스템 python3에 없음 → `/opt/homebrew/bin/python3.11` 사용 (파이프라인 Phase에서 전용 venv로 정리 예정)
- [x] 설계 결정: brush 이미지 없으면 커서를 그리지 않음 (참조의 만화 펜 폴백은 채택 안 함 — 단순화)

### 완료 조건
- [x] 구현 완료
- [x] 자체 테스트 완료
- [x] 골든 파리티 게이트 통과 (최대 0.174% ≤ 2%)
- [x] Phase 완료 커밋 (메시지에 Phase 번호 명시)

## 잔여 리스크 / 후속 과제

- 본 문서 완료 후 후속 워크스트림: 연출 레이어(계획서 Phase 2)와 Python 파이프라인(계획서 Phase 3)은 병렬 가능 — 각각 새 dev-plan 파일로 진행
- "수묵화 느낌" 재현이 2% 게이트로 부족하다고 판단되면 기준 프레임 수를 늘리는 것으로 대응 (게이트 완화는 금지)
